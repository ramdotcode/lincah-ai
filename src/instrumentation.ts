// Sentry is initialized via sentry.client.config.ts and sentry.server.config.ts
// This file is required by Next.js but can be empty for Sentry integration via withSentryConfig
export function register() {
  // Initialization is handled by @sentry/nextjs wrapper
}
